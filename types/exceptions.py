"""
Exceptions Module

This module defines custom exceptions for our application.
These exceptions provide a structured way to handle specific error conditions
throughout the application.

Usage:
    from utils.exceptions import DatabaseError, APIError

    raise DatabaseError("Failed to connect to the database")
"""

